[project]
name = "vulnexus"
version = "1.0.0"
description = "AI-powered vulnerability research platform backend"
requires-python = ">=3.11"
dependencies = [
]

[project.optional-dependencies]
test = [
    "pytest==8.3.4",
    "pytest-asyncio==0.24.0",
    "httpx==0.28.1",
]

# [build-system]
# # requires = ["hatchling"]
# # build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = [
    "pytest==8.3.4",
    "pytest-asyncio==0.24.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
